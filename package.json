{
  "name": "clearsky-weather",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "node server.js",
    "build": "mkdir -p dist && cp *.html style.css script.js dist/ 2>/dev/null || true",
    "start": "node server.js",
    "lint": "echo 'No linting required'"
  },
  "dependencies": {
    "express": "^4.21.2"
  }
}
